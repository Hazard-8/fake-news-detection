# Models Folder

The trained model files are not uploaded to GitHub:

```text
model.pkl
vectorizer.pkl
```

To create them, first add the dataset, then run:

```powershell
python python_files\combine.py
python python_files\model.py
```
