# Data Folder

These files are not uploaded to GitHub:

```text
database.db
Fake.csv
True.csv
news.csv
```

Add your own dataset files:

```text
Fake.csv
True.csv
```

Both files must have a `text` column.

Then run:

```powershell
python python_files\combine.py
```
